using UnityEngine;
using SafeDriver.Core;

namespace SafeDriver.Scoring
{
    /// <summary>
    /// Tracking de progreso meta: niveles desbloqueados, achievements, persistencia.
    /// </summary>
    public class ProgressionManager : MonoBehaviour
    {
        // TODO: SaveData via PlayerPrefs o JSON local, unlock criteria
    }
}
